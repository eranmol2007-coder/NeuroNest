/**
 * Validators Index
 * Input validation functions
 * @module validators
 */

const patientValidator = require('./patientValidator');
const gameScoreValidator = require('./gameScoreValidator');

module.exports = {
  patientValidator,
  gameScoreValidator,
};
